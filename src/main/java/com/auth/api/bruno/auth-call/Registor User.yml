info:
  name: Registor User
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/auth/register
  body:
    type: json
    data: |-
      {
        "username":"Admin",
        "password": "Admin@123",
        "email":"admin@email.com",
        "fullName":"Admin Login",
        "applicationCode":"ADM",
        "expiryDate": "2031-12-31T23:59:59",
        "roles": ["admin"]
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
